import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'trabajo.ionic',
  appName: 'trabajo-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
